name: gatewayserver CI

on:
  push:
    paths:
      - 'gatewayserver/**'
      - 'eak-bom/**'
  workflow_dispatch:


jobs:
  build-test-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Install BOM
        working-directory: eak-bom
        run: mvn clean install -DskipTests

      - name: Build and Test
        working-directory: gatewayserver
        run: mvn clean verify

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker image
        run: |
          docker build -t georgeestav/gatewayserver:${{ github.sha }} ./gatewayserver
          
          docker tag georgeestav/gatewayserver:${{ github.sha }} georgeestav/gatewayserver:latest
          
          docker push georgeestav/gatewayserver:${{ github.sha }}
          docker push georgeestav/gatewayserver:latest