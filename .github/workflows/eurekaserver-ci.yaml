name: eurekaserver CI

on:
  push:
    paths:
      - 'eurekaserver/**'
      - 'eak-bom/**'
  workflow_dispatch:


jobs:
  build-test-push:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v5

      - name: Setup JDK 21
        uses: actions/setup-java@v5
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: maven

      - name: Install BOM
        working-directory: eak-bom
        run: mvn clean install -DskipTests

      - name: Build and Test
        working-directory: eurekaserver
        run: mvn clean verify

      - name: Login to Docker Hub
        uses: docker/login-action@v4
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Build and Push Docker image
        run: |
          docker build -t georgeestav/eurekaserver:${{ github.sha }} ./eurekaserver
          
          docker tag georgeestav/eurekaserver:${{ github.sha }} georgeestav/eurekaserver:latest
          
          docker push georgeestav/eurekaserver:${{ github.sha }}
          docker push georgeestav/eurekaserver:latest